import { Button as EnloveButton } from "@rx-lab/enlove-ui";
import "@rx-lab/enlove-ui/styles.css";

/** Primary UI component for user interaction */
export const Button = () => {
  return (
    <div>
      <EnloveButton />
    </div>
  );
};
